# wsl - WSL2 Mihomo 独立部署覆写
# 在 SubStore 中作为额外覆写使用，给 mainline.min.js 生成的配置补充顶层控制字段
# 运行顺序：先跑主 override JS（mainline.min.js），再跑此 YAML
# jsDelivr: https://cdn.jsdelivr.net/gh/Zer0wAve/zer0plan@main/wsl.yaml

# ===== Mihomo 控制字段 =====
mixed-port: 15396
allow-lan: true
bind-address: '*'
mode: rule
log-level: info
external-controller: 0.0.0.0:15397
# secret: 本地配置时自行填写，勿提交到仓库
external-ui: /home/hululu/workspace/mihomo/ui
dns:
  nameserver-policy:
    '+.day.app': '223.5.5.5'

# 桌面专用进程直连规则，不加入 iOS Stash 共用 JS
+rules:
  - 'PROCESS-NAME,v2ray,DIRECT'
  - 'PROCESS-NAME,Surge,DIRECT'
  - 'PROCESS-NAME,ss-local,DIRECT'
  - 'PROCESS-NAME,privoxy,DIRECT'
  - 'PROCESS-NAME,trojan,DIRECT'
  - 'PROCESS-NAME,trojan-go,DIRECT'
  - 'PROCESS-NAME,naive,DIRECT'
  - 'PROCESS-NAME,CloudflareWARP,DIRECT'
  - 'PROCESS-NAME,Cloudflare WARP,DIRECT'
  - 'PROCESS-NAME,p4pclient,DIRECT'
  - 'PROCESS-NAME,qbittorrent,DIRECT'
  - 'PROCESS-NAME,Transmission,DIRECT'
  - 'PROCESS-NAME,aria2c,DIRECT'
  - 'PROCESS-NAME,fdm,DIRECT'
  - 'PROCESS-NAME,uTorrent,DIRECT'
  - 'PROCESS-NAME,WebTorrent,DIRECT'
  - 'PROCESS-NAME,Thunder,DIRECT'
  - 'PROCESS-NAME,DownloadService,DIRECT'
